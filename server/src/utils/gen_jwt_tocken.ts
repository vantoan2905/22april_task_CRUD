// import jwt from 'jsonwebtoken';

// const generateJwtToken = (payload: object, secretKey: string, expiresIn: string | number): string => {
//     return jwt.sign(payload, secretKey, { expiresIn });
// };

// // Example usage
// const payload = { userId: 123, username: 'exampleUser' };
// const secretKey = 'your_secret_key'; // Replace with a secure secret key
// const token = generateJwtToken(payload, secretKey, '1h'); // Token expires in 1 hour

// console.log('Generated JWT Token:', token);